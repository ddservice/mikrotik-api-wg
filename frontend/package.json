{
  "name": "mikrotik-dashboard-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Vue 3 + Vite frontend (pilot). Builds to ../public/v2 — the VPS never installs these deps.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.7"
  }
}
